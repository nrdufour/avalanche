---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: wallabag-external-store
spec:
  configuration:
    destinationPath: s3://cloudnative-pg/
    endpointURL: https://s3.garage.internal
    endpointCA:
      name: cnpg-garage-access-wallabag
      key: tls.crt
    s3Credentials:
      accessKeyId:
        name: cnpg-garage-access-wallabag
        key: aws-access-key-id
      secretAccessKey:
        name: cnpg-garage-access-wallabag
        key: aws-secret-access-key
    data:
      compression: bzip2
    wal:
      compression: bzip2
      maxParallel: 8
