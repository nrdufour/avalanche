# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config.yaml
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  # Note: PVC is managed by volsync-v2 component
components:
  - ../../../components/volsync-v2
## dataSourceRef patch temporarily removed for LINSTOR migration restore
## Re-add after PVC is bound with data from snapshot
