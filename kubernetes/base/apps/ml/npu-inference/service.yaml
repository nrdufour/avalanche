apiVersion: v1
kind: Service
metadata:
  name: npu-inference
  labels:
    app.kubernetes.io/name: npu-inference
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: npu-inference
