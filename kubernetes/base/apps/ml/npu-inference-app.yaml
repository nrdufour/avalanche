apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: npu-inference
  namespace: argocd
spec:
  project: default
  source:
    path: kubernetes/base/apps/ml/npu-inference
    repoURL: https://forge.internal/nemo/avalanche.git
    targetRevision: HEAD
  destination:
    namespace: ml
    name: 'in-cluster'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
