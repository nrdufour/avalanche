# yaml-language-server: $schema=https://json.schemastore.org/kustomization
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home-automation
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - code-server-service.yaml
  - code-server-ingress.yaml
  - db

components:
  - ../../../components/volsync-v2

# NOTE: dataSourceRef-removal patch temporarily removed for the LINSTOR→Longhorn
# migration (2026-04-29) so the new PVC restores from the latest restic backup.
# Re-add after the new PVC binds and home-assistant is verified healthy.
