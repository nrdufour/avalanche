apiVersion: v1
kind: Service
metadata:
  name: ircnet-healthcheck
  namespace: irc
spec:
  selector:
    app: ircnet-healthcheck
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
