apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kyverno
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://forge.internal/nemo/avalanche.git
      targetRevision: HEAD
      ref: values
    - path: .
      repoURL: oci://ghcr.io/kyverno/charts/kyverno
      targetRevision: 3.7.0
      helm:
        releaseName: kyverno
        valueFiles:
          - $values/kubernetes/base/infra/kyverno/kyverno/helm-values.yaml
  destination:
    namespace: kyverno
    name: 'in-cluster'
  ignoreDifferences:
    # Kyverno 3.7.0 CRDs have empty labels/annotations in chart but not in cluster
    - group: apiextensions.k8s.io
      kind: CustomResourceDefinition
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
